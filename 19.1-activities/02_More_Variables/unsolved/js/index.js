// Create the addNumbers function here.

var peopleup = 2;
var peopledown = 3;
var z = 'There are ' + (peopleup+peopledown) + ' people all together';

// Create the stringConcat function here.

function addNumbers() {
    console.log(z);
     
}

addNumbers();

